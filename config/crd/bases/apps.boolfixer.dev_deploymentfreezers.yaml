---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: deploymentfreezers.apps.boolfixer.dev
spec:
  group: apps.boolfixer.dev
  names:
    categories:
    - all
    kind: DeploymentFreezer
    listKind: DeploymentFreezerList
    plural: deploymentfreezers
    shortNames:
    - df
    singular: deploymentfreezer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.name
      name: Target
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.freezeUntil
      name: FreezeUntil
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              durationSeconds:
                description: Duration of the freeze window in seconds. After this
                  period, the operator restores the Deployment.
                format: int64
                minimum: 1
                type: integer
              targetRef:
                description: Target Deployment reference.
                properties:
                  name:
                    description: Name of the target Deployment (same namespace as
                      this CR).
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - durationSeconds
            - targetRef
            type: object
          status:
            properties:
              conditions:
                description: Fine-grained condition set.
                items:
                  properties:
                    lastTransitionTime:
                      description: RFC3339 time of the last status change.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message (for operators/users).
                      maxLength: 2048
                      type: string
                    reason:
                      description: Short CamelCase reason for the last transition.
                      enum:
                      - Found
                      - NotFound
                      - UIDMismatch
                      - Acquired
                      - DeniedAlreadyFrozen
                      - Lost
                      - Released
                      - ScalingDown
                      - ScaledToZero
                      - AwaitingPDB
                      - ScalingUp
                      - ScaledUp
                      - QuotaExceeded
                      - PartialRestore
                      - Normal
                      - Degraded
                      - APIConflict
                      - RBACDenied
                      - Observed
                      type: string
                    status:
                      description: Whether the condition is satisfied.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: Category of fact.
                      enum:
                      - TargetFound
                      - Ownership
                      - FreezeProgress
                      - UnfreezeProgress
                      - Health
                      - SpecChangedDuringFreeze
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              freezeUntil:
                description: Absolute time when the Deployment should be unfrozen.
                format: date-time
                type: string
              observedGeneration:
                description: Last observed generation of the CR's spec.
                format: int64
                type: integer
              originalReplicas:
                description: Replicas before freezing (for deterministic restore).
                format: int32
                type: integer
              phase:
                description: High-level lifecycle summary.
                enum:
                - Pending
                - Freezing
                - Frozen
                - Unfreezing
                - Completed
                - Denied
                - Aborted
                type: string
              targetRef:
                description: Cached target info recorded when the freeze started.
                properties:
                  name:
                    description: Cached name of the target Deployment.
                    minLength: 1
                    type: string
                  uid:
                    description: |-
                      UID of the Deployment at the time the freeze began
                      (detects delete+recreate under the same name).
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
